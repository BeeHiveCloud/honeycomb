# honeycomb - git module

CMAKE_MINIMUM_REQUIRED(VERSION 3.1)

FILE(GLOB SRC_MYGIT *.c *.h)
ADD_LIBRARY(mygit STATIC ${SRC_MYGIT})
ADD_DEPENDENCIES(mygit git2)
ADD_DEPENDENCIES(mygit mysqladapter)
TARGET_LINK_LIBRARIES(mygit git2)
TARGET_LINK_LIBRARIES(mygit mysqladapter)
