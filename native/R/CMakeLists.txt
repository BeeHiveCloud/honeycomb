# honeycomb - R module

CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

FILE(GLOB SRC_ROGER *.h *.c)
ADD_LIBRARY(radapter STATIC ${SRC_ROGER})
TARGET_LINK_LIBRARIES(radapter R)
TARGET_LINK_LIBRARIES(radapter Rgraphapp)
